import { ProtectedRoutesTemplate } from "@components/common/templates/protected-routes-template";

const Dashboard = () => {
    return <ProtectedRoutesTemplate>content</ProtectedRoutesTemplate>;
};

export default Dashboard;
