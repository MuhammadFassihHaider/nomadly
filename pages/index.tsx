import { DarkModeButton } from "@components/common/dark-mode-button";
import React from "react";

const Home: React.FC = () => {
    return (
        <div>
            <DarkModeButton />
        </div>
    );
};

export default Home;
